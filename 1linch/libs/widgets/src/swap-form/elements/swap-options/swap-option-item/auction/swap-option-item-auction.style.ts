import { css } from 'lit'

export const swapOptionItemAuctionStyle = css`
  .auction-value {
    color: var(--primary);
    text-align: right;
    font-size: var(--font-size);
    font-style: normal;
    font-weight: 500;
    line-height: 24px;
  }

  .editable {
    cursor: pointer;
  }
`
